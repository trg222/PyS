const path = require ('path');

module.exports ={
    
    //Esta será la accion que ocurrirá al ir a /users/login en el sitio web
    login : (req, res) =>{
        //código
    },

    //Esta será la accion que ocurrirá al ir a /users/register en el sitio web
    register : (req, res) =>{
        //código
    }
}